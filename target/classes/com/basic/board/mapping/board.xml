<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
	
<sqlMap namespace="board">
	<insert id="insertBoard" parameterClass="boardVO">
		insert into board(board_no, board_title, board_content, board_writer, board_view, board_regdate)
			values(#board_no#, #board_title#, #board_content#, #board_writer#, #board_view#, sysdate)
	</insert>
	
	<select id="getBoardList" resultClass="boardVO">
		select * from board order by board_no desc
	</select>
</sqlMap>